JWT_SECRET=verander-dit-naar-een-lang-geheim-wachtwoord
JWT_EXPIRES_IN=30d
PORT=3001
NODE_ENV=production
DB_PATH=./data/wkpool.db

# Credentials for the first admin account, only used when seeding a fresh
# database. Leave unset to fall back to the legacy default (william / admin123),
# but setting them keeps real credentials out of source control.
ADMIN_USERNAME=william
ADMIN_PASSWORD=verander-dit-naar-een-sterk-wachtwoord
